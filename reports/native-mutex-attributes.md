# Native mutex attributes

2026-09-07 01:16 UTC. The first constructor now successfully initializes native mutex attributes and sets type 2. Startup stops at scePthreadMutexInit, NID cmo1RIYva9o, gateway 0x102bbfec8, after 154 completed registered memory operations. Constructor ordinal 0 remains incomplete; P4 is open and no native boot or playable port exists.

Three native attribute services now have stateful implementations: initialization creates a distinct process-owned object, settype changes that object's value, and destruction releases it and clears the supplied handle slot. Nine exact canonical/PLT import identities are bound in the probe's derived table. NID/library/module/version matching uses the existing canonical registry identities; the base registry and all game AOT objects remain unchanged. The contract is local/runtime/mutexattr-contract-v1/contract.json. Mutex creation, locking, other attribute setters and POSIX-namespace aliases remain unimplemented.

Attribute handles occupy reserved logical identifier range [0x71000000000,0x71100000000), checked against loaded regions and compiled targets. They never expose host pointers. The handle slot is eight bytes; private attribute state lives in a native process table. The supplied caller passes that slot by reference for initialization, type selection, mutex creation and destruction. Direct guest reads of the private attribute layout remain explicit memory stops. This is a bounded native service interface, not an inferred complete private structure layout.

The native default is type 1, protocol 0, ceiling 0, matching the pinned baseline reference; the reached caller explicitly replaces type with 2 before mutex creation. Types 1–4 are stored, and invalid inputs return 0x80020016. Resource exhaustion returns 0x8002000c without changing the output slot. A limit of 4,096 simultaneously live attribute objects and monotonic, non-reused identifiers are native implementation limits. Null/invalid diagnostics and live reinitialization rejection are documented native policies. The source constructor/helper independently compares the SCE invalid/allocation error encodings, but does not establish every failure case or the default policy.

Ghidra independently decodes 91 instructions across the constructor, full bounded mutex helper and four service stubs. Pointer setup, type 2, call targets and error checks agree with the supplied bytes and Capstone lengths. The [FreeBSD 9.0 mutex-attribute implementation](https://raw.githubusercontent.com/freebsd/freebsd-src/releng/9.0/lib/libthr/thread/thr_mutexattr.c) independently shows allocation, copied defaults, mutable attributes and destruction through a pointer-valued attribute interface. Its local copy is local/references/mutexattr-v1/thr_mutexattr.c, SHA256 ab993c51f0c2c16de88e61d8e16c2df81621d076939dfa75f36bee376b5e5854. The Open Group [attribute rationale](https://pubs.opengroup.org/onlinepubs/9699919799/xrat/V4_xsh_chap02.html) describes opaque attributes and how type selection affects subsequently created mutexes. Neither reference constitutes a console observation; the native implementation uses its own logical handle table.

Repeated authored AOT checks pass 45,059 service calls across 4,096 lifecycles, including independent objects, all supported type values, invalid type preservation, allocation exhaustion, stale handles, destruction and surrounding-byte preservation. Callee-saved registers, 32-bit return values and logical call/return stack behavior are checked. Four negative processes stop on a bad pointer, absent provider, live reinitialization and direct opaque-handle dereference. All objects and results repeat exactly.

Current probe local/runtime/startup-v9-mutexattr-repeat/startup.exe matches v8-mutexattr exactly: 61,822,976 bytes, SHA256 e43bce2608fb9fbe9c420d60e26845c565359277a2a533c58742303f64e2fc81. Seventeen call/import events repeat, with init and settype returning zero. The next call records RDI=0x1056a5728 (mutex output slot), RSI=0x700000fff48 (attribute slot), RDX=0 (no name), RSP=0x700000fff38. The actual imported service stops with reason 25. Only Windows thread ID is excluded from repeated fault comparison.

Bundle 9e853802836c7118f7701b5679a3ac6154599ff066b56a0d6008da0ff573a0e8; trace identity d7faa7a309dcfb81e941e56ff4260daba2683a483a1744e7ec6ffd9b9ceec48b; seed SHA256 fd3936a99e3629eda04843c671e3776e23e7ef82b08dc698c0a05326e489d9e7. Loader-plan-v9-runtime-word, the 29 guarded data/TLS slots, native-memory-manifest-v1, registry-v7-memory-repeat, compiler v12 and semantics v35 remain unchanged. Source byte mappings remain NX. Evidence local/runtime/mutexattr-evidence-v1/checked.json and reports/native-mutex-attributes-evidence.json verifies all recorder source snapshots. Complete initialization order, FP/TLS, guest exceptions/nonlocal control, unknown targets, private object layouts and helper performance remain open. P1 baseline route/profiling/audio work remains separate.

Next implement and test native mutex creation, ownership and synchronization against the reached recursive-mutex contract, then investigate the next actual startup stop. No user action is needed.
