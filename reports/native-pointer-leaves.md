# Native pointer-sequence accessors

2026-09-07 03:04 UTC. Repeated native startup completes constructors 0–96 and enters ordinal 97. Six additional checked accessors execute and return: three recovered from successive actual requests, followed by three compiled together from a bounded initial pointer sequence. The next unknown target is 0x10236f6f0. P4 remains open; no native boot or playable port exists.

The accessor-chain-v2 workflow recovered 0x102370950, 0x102370960 and 0x102370970 only after each was requested by startup. Independent Ghidra decoding, repeated 3,072-case AOT fixtures, additive manifests and exact startup repeats pass for every step. The first two load 64-bit fields; the third returns a logical address.

At the next stop, diagnostic RAX provided a candidate initial pointer sequence. Recovery examined thirteen consecutive relative-relocation slots, within an explicit sixteen-slot limit. Existing compiled entries, unsupported bodies and the three exact LEA/RET forms were recorded separately. The requested entry 0x102370980 was observed; 0x102370990 and 0x1023709a0 remained conditional initial-pointer dependencies when compiled. This is not proof of a vtable type, caller instruction, complete table extent or immutable contents. Unsupported bodies and canonical unresolved recovery records remain unchanged.

Ghidra independently agrees with all three bodies and RIP-derived addresses. The grouped Remill/LLVM build exports exactly their roots, has no new imports or source-transfer pairs, and uses sourced memory only for logical RET reads. Two builds reproduce the object, bitcode, instruction checks and native test objects. The fixture passes 9,216 authored input cases and six guarded/unmapped return checks. These are game-derived AOT bodies tested in synthetic contexts, not a claim that candidate pointers prove execution.

The subsequent actual startup trace separately establishes native returns from all three: 0x102370980 returns 0x1056c54a2, 0x102370990 returns 0x1056c54a1 and 0x1023709a0 returns 0x1056c54a0. Returning a logical address does not establish the pointed-to object's identity or layout. All six newly added bodies appear in the trace.

Current local/runtime/startup-v39-pointer-leaves-repeat matches v38-pointer-leaves. Its 61,861,888-byte executable has SHA256 a578469e2f2a847a7fcae9dd98e06e2c431bd7aed0f0b2e5a353e37c8f0051fe. Eleven supplements produce 397 game objects / 21,295 compiled roots. Complete compilation manifests, native objects, bundle, seed and 415 dispatch/import events repeat exactly; only Windows thread ID is excluded from fault comparison. The last dispatch requests 0x10236f6f0 with RSP 0x700000ffea8 after 2,726 completed registered memory operations. The trace does not enumerate every static call or instruction.

Evidence local/runtime/pointer-leaves-evidence-v1/checked.json verifies chain artifacts, constructor order, per-address returns, recorder source archives, original module hashes and the unchanged canonical recovery database. Source commits and tools/run_record.py records preserve reproduction. The reusable tools/extend_native_startup.py clones a passed recorder command into a fresh output, retaining every service/TLS/supplement argument.

Continue using record 20260907-p4-startup-pointer-leaves-v2-repeat. Recovery local/cfg/pointer-leaves-v2 and independent Ghidra windows local/cfg/ghidra-pointer-leaves-v2 identify six supported candidates around the new actual request; native compilation validation is in progress. Base registry-v7, loader-plan-v9-runtime-word, rwlock-contract-v1, main-tls-contract-v1 and replay seed remain current. Original bytes remain NX and thirty guards remain. Mutable tables, alternate boundaries, embedded data, dynamic TLS/TCB, guest threads, startup/FP/control closure, exceptions/nonlocal flow and runtime costs remain unresolved. Independent P1 route/profiling/audio work is unchanged. No user action is needed.
