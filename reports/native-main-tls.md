# Native main static TLS and explicit FS state

2026-09-07 02:26 UTC. Native startup passes its first FS:[0] dependency after main static TLS initialization. It continues inside constructor ordinal 89 to another unknown compiled target, 0x102375af0. Constructors 0–88 remain completed. P4 is open; no native boot or playable port exists.

The supplied main PT_TLS declares 1,872 bytes, alignment 16, and no initialized file bytes. All 328 recovered FS-based instructions are MOV RAX,qword ptr FS:[0], immediately followed by an ADD using a source data offset word. Ten distinct initial offsets range from -1864 to -1664; each lies within the main image immediately below the thread pointer. The reached offset at RVA 0x53e44a8 is -1824, addressing template byte 48. None of these offset slots has a relocation. They remain mutable data, so the inventory proves initial contents only. Ghidra independently verifies ten instructions across three bounded FS/offset/use windows.

The [FreeBSD amd64 thread definition](https://raw.githubusercontent.com/freebsd/freebsd-src/releng/9.0/lib/libthr/arch/amd64/include/pthread_md.h) places a self pointer at FS offset zero. Its [runtime TLS allocator](https://raw.githubusercontent.com/freebsd/freebsd-src/releng/9.0/libexec/rtld-elf/rtld.c) initializes storage below the thread pointer from a module template and zero tail. These independent references agree with the supplied main access pattern and the separately inspected pinned baseline. They do not establish the complete PS4 TCB, dynamic-module layout or scheduling behavior.

The bounded native contract chooses logical TCB 0x74000010000. Its 1,872-byte main TLS image ends at that address, and the first eight TCB bytes contain the logical self pointer. A separately guarded 56-byte envelope reserves the remaining TCB area without supplying guessed DTV, thread or fiber fields. Native initialization writes the template/zero tail and self pointer, then changes only the explicit Remill State FS base. Windows host FS is never changed. Reinitialization, a nonzero prior State FS base, invalid alignment/template and active import/source contexts are rejected.

Repeated authored AOT tests pass 3,752 calls across eight native threads. Every 1,872-byte image is checked for initial template and zero fill, every aligned word is written/read through the compiled FS path, and all thread images retain distinct final values. The fixture checks self pointers, callee-saved registers, logical return/RSP and unchanged FS selection across calls. Three negative processes verify missing FS, an access below the TLS mapping and guarded TCB fields. Duplicate initialization rejects. The initial fixture build failed because SIZE collided with a Windows type name; that recorded run is retained. Renaming the fixture constant to TLS_BYTES produced exact repeated successful native objects and results.

local/runtime/main-tls-contract-v1/contract.json binds the supplied module, loader and registry identities, the complete recovered FS inventory, independent checks and repeated authored tests. Add --tls local/runtime/main-tls-contract-v1 to the existing probe command. Startup includes the contract and additional mapping/guard in its compilation and bundle identity, validates the private initial image, then initializes TLS before entry. All 29 previous data/TLS guards remain; the TCB guard makes 30 total. Unresolved DTPMOD64 references and dynamic TLS are unchanged.

Current local/runtime/startup-v19-main-tls-repeat/startup.exe repeats v18-main-tls: 61,860,352 bytes, SHA256 a515938691532404e0363d863af62e914df7f243b1450d5b9cec09144ca4d40b. Trace identity 989ca574240bd8a20d4c3bf1c306957e9dccff74f59489412e7e5b142b1dea69; bundle SHA256 8b85151f691d368c76bfa32db47d7bfa8160afbb1fed07b17a0525ddfaec4eef. All native objects, bundle, seed and 315 diagnostic dispatch/import events repeat, excluding only Windows thread ID from fault comparison. The next missing target stops at RSP 0x700000ffea8 after 1,748 completed registered memory operations. This trace does not enumerate every compiled direct call or instruction, and the 328 static FS sites are not execution coverage.

Evidence local/runtime/main-tls-evidence-v1/checked.json verifies exact artifacts, supplied module hashes and recorder/source archives. Compiler v12-memory-sources, semantics v35-divide, registry-v7-memory-repeat and native-leaf-manifest-v1 remain current: 387 game objects and 21,283 roots. All original bytes remain NX. Main TLS is one checked initialization component; full module initialization order, dynamic TLS/TCB fields, guest thread creation, FP profile, unknown targets, mutable tables, exceptions/nonlocal control and helper costs remain open. P1 route/profiling/audio work is separate and unchanged.

Next recover the observed target 0x102375af0, independently decode it and extend the native compilation manifests before another probe. No user action is needed.
