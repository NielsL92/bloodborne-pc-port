# Private native module loading

Later execution checkpoint (2026-09-07 00:15 UTC): reports/native-startup.md records the first bounded supplied-entry AOT trace and its repeated canary-slot stop. Earlier no-game-execution statements below describe their dated experiments. No native boot or playable port exists.


2026-09-06 23:55 UTC. P4 remains open. P3 compilation/explicit handling remains passed under its narrow recorded scope. No game-derived CPU function, initializer or callback has executed. There is no native boot or playable port.

The native loader now reads a SHA256-checked bundle tied to the current registry identity, copies sixteen module regions into private NX backing, zeroes BSS/alignment tails and applies 238,572 explicitly resolved relocations. All 37 unresolved destinations remain named access guards: twenty strong data references and seventeen TLS module references. No guessed semantic value is assigned. Native service slot 9 remains excluded from module mappings. Resolved values retain the existing static namespace/version/interposition assumptions.

The parser rejects malformed counts/sizes, overlapping mappings or relocation destinations, relocation writes outside declared writable data, guards overlapping resolved slots, native slot collisions, truncated/trailing bundles and hash/registry identity mismatch. All mappings are sealed with registered logical rights and non-executable host protection. This standalone validator links only native loader/runtime support; it contains no game AOT roots to call.

An independent Python calculation and the native reader agree on 97,517,272 unguarded private bytes out of 97,517,568 mapped bytes. Their digest is cebfd46360e542e8cf9a1fa0f3b260943be96da857133345a8a6115bc1b8628b. The 296 guarded bytes are excluded from comparison and meaning, while every other file/BSS/alignment/relocation byte is checked. Thirty-seven fresh child processes each stop before completing a read of their assigned unresolved slot. The earlier authored runtime guard tests also cover writes, CAS, atomics and cross-region accesses.

Authored mappings pass, and eleven malformed bundle/identity cases produce the required rejections. load-v3-repeat exactly matches load-v2-sha-width for all bundles, native objects and validation outputs. Standalone fixture PE timestamps are not normalized. The first compile failure (an unavailable Windows limit macro) remains preserved as load-v1; the width check uses UINT32_MAX and the successful runs retain their own source snapshots. All eight supplied module hashes are rechecked and unchanged. Original packages and hardlinked game views were never written.

Current bundle: local/runtime/load-v3-repeat/modules.bin, SHA256 f2f0ce06c0b96892e3d1634c25e3542a3931189ceec4730352d4bbf3fcc0207a. Plan: local/runtime/loader-plan-v8-memory. Registry: local/runtime/registry-v7-memory-repeat, identity e2b4ebb39d380c389b53da374442030e43e79f2109d61a252f893b0ecbe6f07e. Evidence: reports/native-load-evidence.json and local/runtime/load-evidence-v1/checked.json. The complete AOT-linked host validator remains local/runtime/link-v9-memory-repeat/registry.exe; its entry mode is still disabled.

Next independently check actual entry arguments and initialization order, add logical stack/TLS and precise native service contracts, and investigate the first bounded native startup stop. Current compiler/objects are native-memory-manifest-v1 with v12-memory-sources and semantics v35-divide. Target FP profile, fourteen x87 selector sites, conditional control/export assumptions, unknown targets, guest exceptions/nonlocal transfers and helper runtime cost remain open. Do not manually call the initial constructor table: compiled initialization must observe its mutable data. P1 route/profiling/audio work remains separate.
