# Observed native target recovery and manifest extension

2026-09-07 02:04 UTC. The missing target 0x10207f3b0 now executes as native AOT and returns to 0x10207f2bf. Startup then reaches scePthreadRwlockInit, NID 6ULAa0fq4jA, at 0x102bc0cd8 inside constructor ordinal 89. Constructors 0–88 remain completed. P4 stays open; no native boot or playable port exists.

The supplied main-module body is exactly MOV AL,byte ptr [RDI+0x10] followed by RET: bytes 8a4710c3 at RVA 0x207f3b0. A bounded linear decoder stops at that RET and includes neither following padding nor the adjacent entry at 0x207f3c0. Ghidra independently decodes both instructions from the original module bytes without receiving expected instructions. The existing recovery database lacks these instruction starts. A four-byte unwind range and two relocation pointer candidates corroborate the location but remain metadata and candidate evidence, not function-boundary or table-mutability proofs. The actual dispatch establishes a requested entry, not its dynamic caller instruction.

Compiler v12-memory-sources and semantics v35-divide lift the body with explicit native memory provenance. Independent LLVM inspection verifies both sourced reads, one byte load and the return-address read, and finds no indirect LLVM calls or missing blocks. COFF exports only sub_10207f3b0 and references only the two registered memory reads plus the native return helper. Two builds reproduce the input, audit, bitcode, object, inspection and native fixture objects exactly.

The supplied compiled body passes 3,072 synthetic input cases: all 256 byte values, four initial RAX patterns and three object positions including the last mapped byte. The upper RAX bits, selected flags, callee-saved registers, object pointer and logical return/RSP are checked. Unmapped and unresolved byte reads stop at source 0x10207f3b0 before reading. These are game-derived AOT function experiments with authored input contexts, distinct from emulator baseline execution and full startup.

local/compiler-spike/native-leaf-manifest-v1/manifest.json adds one object, one entry/root and two instructions to the preserved base. The combined startup build has 387 game objects and 21,283 compiled roots. Base registry-v7-memory-repeat and the full original recovery artifacts remain unchanged. The additive manifest binds the source module, recovery database, observed probe, Ghidra check, compiler/semantics, independent LLVM inspection, exact repeat and native fixture result. Startup verifies those artifacts and emits complete compilation-objects.json and compilation-targets.json. Both manifests and the extension enter the probe identity before any game entry executes. No imports or source/request transfer pairs are added. Native table validation checks the combined target set.

Current local/runtime/startup-v15-native-leaf-repeat/startup.exe repeats v14-native-leaf: 61,848,064 bytes, SHA256 b18c60e343551cbe490d23636bfe7a242aaeeb9641354a1cabf9a55e161dcf6d. Trace identity de639c58b3dac85023459393bdc0a830c201a5496cd2ff520fffcddee556416b. All native objects, bundle, seed and 253 diagnostic dispatch/import events repeat; only Windows thread ID is excluded from fault comparison. The new leaf returns after two memory operations, retaining RAX 0x1052b6d00 in this invocation. The next import occurs at 1,208 operations with RDI 0x1056a2090, RSI zero and RSP 0x700000ffed8. The trace still does not list every compiled direct call or instruction.

Evidence local/runtime/native-leaf-evidence-v1/checked.json checks exact artifacts, all eight original module hashes and run-record source archives. Original bytes remain NX; 29 unresolved data/TLS guards remain. Native memory and mutex limitations, uncertain boundaries and targets, mutable tables, full initialization order, FP/TLS, exceptions/nonlocal flow and runtime costs remain open. The narrow earlier compilation/explicit-handling gate does not imply complete target discovery; this observed dependency illustrates why recovery continues during P4. P1 route/profiling/audio work remains separate and unchanged.

Next independently establish the reached reader/writer-lock interface and native ownership behavior, then continue startup. No user action is needed.
