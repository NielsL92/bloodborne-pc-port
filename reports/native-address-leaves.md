# Native address accessors and constructor progress

2026-09-07 02:47 UTC. Native startup completes constructors 0–92 and enters ordinal 93. Four independently checked RIP-relative LEA/RET accessors at 0x102375b10, 0x102375b20, 0x102375b30 and 0x102375b40 execute successfully. Their respective results are 0x1056c5513, 0x1056c5512, 0x1056c5511 and 0x1056c5510. The next unknown target is 0x102370950. P4 remains open; no native boot or playable port exists.

Each eight-byte body is requested by actual native execution before recovery. Ghidra independently agrees with exact source bytes and each RIP-derived address. Compiler v12-memory-sources / semantics v35-divide produces native objects with the logical address embedded through lifted semantics. Independent LLVM inspection confirms that each body reads memory only for its logical return address, at its RET instruction. It does not dereference the returned address. COFF exports only the declared root and references supported native helpers.

Repeated AOT fixtures run 3,072 synthetic input cases per accessor, varying initial registers and object pointers while checking its constant result, flags, callee-saved registers and logical return/RSP. Unmapped and guarded return slots stop at the RET source. The existing 64-bit load form passes a regression with identical input/audit/bitcode/object artifacts. These tests execute supplied compiled bodies with authored input contexts; they do not establish pointed-to object layouts or execution coverage elsewhere.

The bounded tools/advance_native_leaf.py workflow advances at most three actual unknown-target stops per invocation. Every step requires fresh recovery, independent Ghidra decoding, the strict supported-body grammar, two compiled/function-test runs, a manifest, and two complete native startup runs with exact artifact comparison. An unsupported body or failed check stops that sequence for investigation. No adjacent targets are automatically accepted. The completed address-chain-v1 records every child experiment through tools/run_record.py.

Current local/runtime/startup-v31-address-chain-v1-3-repeat/startup.exe has 61,860,864 bytes, SHA256 799263ac542c2d31d3cf2e708d941536558b48adc59a696226618d1a652e4eb9; trace identity cb9199916c56b285d9c7510fcfd7a7770ec8803c4eff0334935598bdd6c399f9. Seven additive manifests bring the combined set to 393 game objects and 21,289 roots. Use the exact continuation_argv in local/runtime/address-chain-v1/summary.json to retain all supplement paths. Native objects, bundle, seed and 387 diagnostic dispatch/import events repeat exactly, excluding only Windows thread ID from fault comparison. The next request has RSP 0x700000ffea8 and 2,325 completed memory operations.

Evidence local/runtime/native-address-leaves-evidence-v2-individual/checked.json validates actual per-accessor results, constructor order, artifacts and recorder/source archives. The preserved first evidence run used an incorrect checker assumption that all four addresses were equal. Correcting the checker to each independently decoded address passes; no compiled body or native trace was changed.

Base registry-v7, loader-plan-v9-runtime-word, rwlock-contract-v1, main-tls-contract-v1 and the recorded seed remain current. All original bytes stay NX; 29 prior data/TLS guards plus the TCB-envelope guard remain. Dynamic TLS/TCB, guest thread creation, complete initialization/FP/control closure, mutable tables, unknown callbacks, exceptions/nonlocal flow and helper costs remain open. P1 route/profiling/audio is separate and unchanged. Continue recovery from the actual request at 0x102370950; no user action is needed.
