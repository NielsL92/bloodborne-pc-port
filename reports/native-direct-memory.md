# Native direct memory and constructor progress

2026-09-07 01:48 UTC. Native startup completes constructors 0–88 and enters ordinal 89 at 0x1023756e0. The first native allocation and mapping succeed: 116 MiB, 2 MiB alignment, memory type zero, physical offset zero, RW mapping with zero flags and address hint. The next stop is an unknown compiled target, 0x10207f3b0, after 1,205 registered memory operations. P4 remains open; no native boot or playable port exists.

The native provider reserves a 4.5 GiB pool and commits backing per successful allocation. This is an explicit native compatibility profile, not a measured console capacity. The 64-bit size query reports the actual native pool budget, 0x120000000. Allocations use page-aligned physical offsets within that pool. Logical mappings occupy [0x1000000000,0x8000000000), share private backing, and remain non-executable. They are added under the address-space lock without replacing original module regions or unresolved-slot guards. Shared ownership retains the reservation until all mappings are gone. Native host pointers never become game pointers. [Windows reservation and commitment semantics](https://learn.microsoft.com/en-us/windows/win32/api/memoryapi/nf-memoryapi-virtualalloc) and [decommit/release requirements](https://learn.microsoft.com/en-us/windows/win32/api/memoryapi/nf-memoryapi-virtualfree) inform this implementation.

The checked interface supports memory type zero, power-of-two alignment of at least 16 KiB, zero-hint flags-zero RW maps, and exact release of an unmapped allocation. Other types, protections, flags, hints, partial or mapped release, unmapping, flexible memory and GPU ownership/coherence remain explicit unsupported interfaces. Native limits and errors are implementation policies. The pinned baseline is a separate reference; its memory-mode constants do not establish console observations or the correct complete native profile.

Ghidra independently decodes 93 instructions across three bounded allocator/PLT windows. Source bytes and Capstone lengths agree. The supplied first caller obtains the size, requests 0x7400000 bytes with 0x200000 alignment and then maps with protection 3. Four exact NID/library/module/version interfaces extend the prior mutex contract: size query, allocation, mapping and release. local/runtime/direct-memory-contract-v1/contract.json contains 31 canonical/PLT bindings for all eleven implemented services. The game trace observes the size, allocation and map calls; it does not establish game execution of the release path.

Repeated authored native AOT tests pass 4,302 service calls, 2,048 allocation/release pairs including nonzero search starts above 4 GiB, 64 shared-alias pairs, seven invalid returns and ten explicit negative stops. Independent alignment arithmetic checks returned offsets; shared mappings agree on writes and remain valid after an external owner is released. Mapping overlap, source-code write protection and existing unresolved guards retain their checks. The RAM regression passes 49,256 cases, including 32,768 atomic tickets across four native threads. These authored tests are separate from supplied game startup and the emulator baseline.

local/runtime/startup-v13-direct-memory-repeat/startup.exe repeats v12-direct-memory exactly: 61,847,552 bytes, SHA256 c5e3aada762bdb1c1f6f06653c1148b87e84cb6b0c951253d63c22f26afb6d25. Bundle SHA256 9e853802836c7118f7701b5679a3ac6154599ff066b56a0d6008da0ff573a0e8; trace identity 2c09e22751a3968456565a5ffafb6d6f26c070b05883e1efcae9fd084391e16e. All native objects, seed, bundle and 251 diagnostic dispatch/import events repeat. Windows thread ID is the sole excluded fault field. The observed constructors return through the actual initializer continuation 0x100000084. The unknown-target fault has RSP 0x700000ffed8 and source field zero; the existing dispatch ABI does not establish its dynamic caller instruction. This trace does not enumerate every statically compiled direct call or instruction.

Evidence local/runtime/direct-memory-evidence-v1/checked.json and reports/native-direct-memory-evidence.json checks recorder/source ZIP identities, exact artifacts and all eight supplied module hashes. Loader-plan-v9-runtime-word, native-memory-manifest-v1, registry-v7-memory-repeat, compiler v12-memory-sources and semantics v35-divide remain current. All original bytes remain NX; 29 strong-data/TLS slots remain guarded. Mutable constructor tables, unknown targets, disputed boundaries, full module initialization order, FP/TLS, guest exceptions/nonlocal flow and helper costs remain open. P1 route, profiler and audio work is unchanged and separate.

Next recover and independently check the actual missing target, compile it through the current Remill/LLVM pipeline and add its exact body to reproducible compilation/registry manifests before the next native probe. No user action is needed.
