# Native startup CFG continuation

2026-09-07 03:21 UTC. Native startup enters the newly compiled body at 0x1020b6e20 and reaches another unknown target, 0x10263a960. Constructors 0–96 are complete; ordinal 97 remains in progress. P4 is open and there is no native boot or playable port.

The preceding pointer-sequence batch adds six independently checked accessors at 0x10236f6f0 through 0x10236f740. Two load 64-bit fields and four return logical addresses. All six pass repeated Ghidra/LLVM checks, 18,432 authored AOT cases and twelve negative memory/return checks; all six subsequently return in startup-v40/v41. The separate evidence file reports this actual execution apart from initial conditional pointer provenance. The resulting request at 0x1020b6e20 is outside the simple-leaf grammar.

The preserved linear-recovery failure correctly rejects the body's branches. tools/recover_native_frontier.py applies the existing CFG recovery engine and exact current annotations to an additive frontier. A fresh copy of original analysis metadata retains constructor order and exception data. Previously compiled roots are external dependencies; canonical startup-recovery-v31 remains unchanged. Both runs produce identical databases, manifests, frontier, windows and overlap evidence. Two entries are recovered: 552 instructions at 0x1020b6e20 and 66 at direct callee 0x1020bccd0. No decode issues or budget queue remain in this bounded delta. Eleven indirect-call records, 34 callback argument candidates, six import contracts, three nonreturn annotations, two unknown restored contexts and other obligations remain explicit. These counts add to the preserved parent frontier; they do not close it.

Ghidra independently agrees with all 618 bytes/instruction records and direct branch/call destinations. It additionally decodes one NOP at RVA 0x20b7641, immediately after the exact existing stack-protector-failure call annotation. The NOP is omitted only under that checked nonreturn contract; the disagreement and its conditional resolution are recorded in independent.json. The metadata range is an analysis fence, not a proven function extent. Exception and nonlocal destinations remain unresolved.

The current compiler v12-memory-sources / semantics v35-divide generates two roots in one additive object. Independent LLVM inspection verifies all register/memory source interfaces and 34 call-return checks, including three exact nonreturn checks. The memory census contains 263 sourced operations. Compilation is recorded as static evidence, distinct from the leaf fixtures' synthetic AOT tests. It does not claim exhaustive function behavior.

The preserved first compilation check stopped on three missing-block records after nonreturn calls. Independent inspection establishes that all three are structurally unreachable from their LLVM entries, and the optimized COFF object has no block-transfer references. The checker now requires that exact relationship: every missing start must equal an annotated next-PC, every corresponding transfer must be unreachable, each fault must have the checked source/reason/continuation, and no transfer reference may survive in COFF. No source-target pair is added. Existing 11 unknown indirect calls retain ordinary checked dispatch. The two subsequent compilation runs reproduce identical artifacts using independently repeated recovery databases.

The manifest and startup tools distinguish independent-cfg-compilation from synthetic leaf validation. Both validate the exact compiler, source, bitcode, instruction checks and object identities. Original imports and source-pair tables remain unchanged; new direct calls resolve only to existing compiled roots/import identities or the new callee. The native loader continues to map original bytes NX with thirty guards.

Current local/runtime/startup-v43-cfg-frontier-repeat exactly matches v42: 61,908,992 bytes, executable SHA256 bd7892af07a94a1398b49a06ec85c194006e2a31a0e2e344c8ed8d4bd8c3368f. Thirteen supplements give 399 game objects / 21,303 roots. The trace has 474 diagnostic events. Constructor progress is checked against the initializer's stack context and return address; source 0 in the unknown-target fault does not identify the actual caller instruction. The last request is 0x10263a960 with RSP 0x700000ffef8 and 3,429 completed registered memory operations. The 0x1020b6e20 entry is observed executing, but has not returned; the new direct helper 0x1020bccd0 remains unobserved.

Evidence local/runtime/native-frontier-evidence-v1/checked.json verifies native objects, complete compilation manifests, traces, source archives and exact recovery repeats. Prior leaf evidence is local/runtime/pointer-batch2-evidence-v1/checked.json. Source tests, static compilation and native trace observations are separate evidence categories.

Continue from recorder 20260907-p4-startup-cfg-frontier-v2-repeat. Fresh local/cfg/pointer-leaves-v3 records supported initial pointer candidates around the actual new request; Ghidra, compiled fixtures and startup-v44/v45 are in progress. Inspect their recorder status before continuing. Full startup ABI/order, FP, dynamic TLS/TCB, guest threads, mutable tables, unknown targets, exceptions/nonlocal behavior and runtime costs remain open. Independent P1 route, profiling and audio investigation is unchanged. No user action is needed.
