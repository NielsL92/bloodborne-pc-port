# Native constructor progress with bounded accessor cohorts

2026-09-07 03:38 UTC. Native startup completes constructors 0–213 and enters ordinal 214. The next unknown target is 0x10207ce10. P4 remains open; no native boot or playable port exists.

A separately checked byte accessor at 0x10263a960 lets the previously added branching body continue and startup complete 104 constructors. The bounded pointer-chain-v1 workflow then adds five six-entry batches, each with Ghidra decoding, repeated native fixtures, a manifest and exact startup repeats. It reaches 120 completed constructors. Every experiment is preserved under tools/run_record.py; the chain records intermediate objects, manifests, observed returns, faults and continuation arguments. Static candidates remain separate from actual returns.

To reduce repeated builds for the same simple forms, tools/census_pointer_leaves.py inspects initial R_X86_64_RELATIVE code-pointer targets in the supplied main module. Of 129,135 distinct initial targets, 18,582 are already compiled at the census checkpoint, 100,793 fall outside the supported forms and 9,760 match exact two-instruction MOV AL,[RDI+disp8], MOV RAX,[RDI+disp8] or RIP-relative LEA RAX forms followed by ordinary RET. Existing recovered instructions and later compiled supplements are excluded conservatively. Unwind metadata, every supplying initial relocation and conditional entry status remain recorded. The census is not a compilation manifest or execution coverage, and does not establish table identity, extent or immutability.

The first cohort selects at most 256 still-uncompiled candidates nearest the actual missing PC, 0x102361580. The earlier five batches leave 9,730 eligible census entries. Proximity selects work only; it does not establish shared semantics, table membership or reachability. Independent Ghidra decodes all 512 instructions, and the established compiler/LLVM checks cover each root and each logical memory-source site. The same two-instruction grammar is retained; no new CPU semantics or fallback is introduced.

The native fixture now chooses three valid field addresses (first mapped bytes, offset 17, last mapped bytes) and derives each object pointer from its decoded displacement. This supports the complete accepted positive disp8 range without accidentally placing a positive case in the intentional middle guard. It does not assert an object layout or require unrelated pointed-to bytes to be mapped. Each body runs 3,072 authored cases across byte patterns, initial register values and field locations. The two builds repeat 786,432 native cases and 512 guarded/unmapped data or logical-return stops, with exact object/bitcode/input/LLVM checks. These synthetic executions remain distinct from the game trace.

The grouped object is added through the normal supplemental manifest. In the subsequent actual startup trace, 144 of its 256 entries return and 112 remain unobserved. The manifest preserves all original conditional flags, including the single entry that had been requested before compilation. Later observations are recorded separately. No constructor percentage or complete callback closure is inferred.

Current local/runtime/startup-v57-pointer-cohort-v1-repeat exactly matches v56. It has 61,946,880 bytes, executable SHA256 36665eb50016d52839f550ae3a0ed4a5cb558f5d0a2be8b76ec4312c29c7f8f1, and trace identity bc5f905198ba637bbe5d2be2b1836b32236ce3b42980c35a00c092885e759cbf. Twenty supplements yield 406 game objects / 21,590 roots. Native objects, complete compilation manifests, source bundle, seed and 2,508 diagnostic events repeat exactly; only Windows thread ID is excluded from fault comparison. The final request has RSP 0x700000ffed8 and 81,916 completed registered memory operations. Source zero remains an unknown caller instruction, not an inferred one.

Evidence local/runtime/pointer-cohort-evidence-v1/checked.json verifies the two builds, actual return subset, constructor stack/return context and recorder archives. The preceding byte-accessor and five-batch evidence is retained separately in pointer-batch3-evidence-v1 and pointer-chain-evidence-v1. All source modules and original assets remain unchanged; source mappings are NX and thirty data/TLS guards remain.

Next recovery local/cfg/native-frontier-207ce10-v1 contains one 24-instruction body with an unknown indirect call and indirect tail jump, and no decode issues. Its repeated recovery, Ghidra windows and native CFG compilation are in progress. Continue from recorder 20260907-p4-pointer-cohort-v1-startup-repeat, preserving every current service/TLS/supplement argument. Canonical recovery and all earlier uncertainties remain intact. Full startup, FP, dynamic TLS/TCB, guest threads, unknown calls, mutable tables, exceptions/nonlocal behavior and helper costs remain open. Independent P1 route/profiling/audio work is unchanged. No user action is needed.
