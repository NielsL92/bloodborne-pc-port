# Native libc runtime word and first constructor

2026-09-07 01:03 UTC. Native startup now completes both atexit registrations and enters the first ordered constructor, 0x1020edf90. That constructor reaches the unimplemented scePthreadMutexattrInit gateway at 0x102bbfea8 after 148 completed registered memory operations. The first constructor has not returned. P4 remains open; no native boot or playable port exists.

The required libc object __stack_chk_guard is provided as one eight-byte process word at logical address 0x70000400000. Exactly eight imports share NID f7uOxY9mM1U, libkernel library version 1 and module version 257. Their symbol size is zero; recovered pointer loads, three independently checked pointer/value prefixes, and the full libc atexit save/compare path establish the bounded eight-byte interface. All 4,224 recovered GOT references are eight-byte pointer loads. Five bundled modules have no recovered pointee-read prefix in this startup set; the exact shared identity supports the binding without claiming those consumers executed. Unknown wider reads, mutation and dynamic interposition remain explicit.

The loader plan is local/runtime/loader-plan-v9-runtime-word. Only those eight relocation values change; twelve other strong-data and seventeen TLS slots remain guarded. The object starts as zeroed storage, then native setup writes a recorded OS-generated eight-byte value before any supplied frame runs. Explicit replay uses the recorded file. No console seed or complete platform initialization policy is inferred. Registered writes remain possible and accesses beyond the logical eight-byte object stop. A changed value takes the compiled mismatch path; __stack_chk_fail remains an explicit unimplemented stop. The general global-word initialization pattern is independently described by [LLVM libc](https://lists.llvm.org/pipermail/libc-commits/2024-January/020237.html); this is an implementation reference, not PS4 evidence.

Authored AOT tests repeat 4,096 successful values, every one-bit mismatch (64 child-process stops), two out-of-bounds stops and a duplicate-initialization rejection. Native OS initialization succeeds without a constant fallback. The first scan's unsupported expectation of eight recovered read prefixes failed; scan-v2-gaps records the actual three and preserves the failed run. Ghidra independently checks those prefixes. The control runtime regression passes 20,480 cases and nine explicit boundary probes after adding optional call tracing.

Both atexit invocations return zero at main continuations 0x1000000c1 and 0x1000000cd. The actual compiled main initializer then follows its mutable constructor table and dispatches ordinal 0, target 0x1020edf90; this is one of the constructor entries lacking unwind-index coverage. The constructor's supplied libc guard-acquire/release imports return, and its direct mutex helper at 0x102083fe0 reaches scePthreadMutexattrInit. Ghidra independently decodes 59 instructions across four bounded constructor/helper/PLT windows, agreeing with source bytes, Capstone lengths and existing recovery rows. These are bounded windows, not complete function extents.

The last native import event records RDI=0x700000fff48 (stack storage for the attribute), RSP=0x700000fff38, and 148 completed memory operations. Native fault reason 25 identifies NID F8bUHwAG284 in libkernel. Its source field is zero because the existing import boundary ABI does not carry the calling instruction; the independently checked helper call is at 0x102084005. Do not claim that source was dynamically reported. The optional diagnostic stream records dispatch/import entries and returns, not every statically linked direct call or every instruction; this is not execution coverage.

Current probe local/runtime/startup-v7-runtime-word-trace-repeat/startup.exe repeats v6-trace exactly: 61,816,320 bytes, SHA256 e63d0d3044433d633cc1ad3156b44c980f31939d7e7382c6391234a89352529f. Bundle SHA256 9e853802836c7118f7701b5679a3ac6154599ff066b56a0d6008da0ff573a0e8; trace identity 90d3a44d40c3ed9e82eb952d293540e3751e8aa5c265d43234d81a3b1b5e85ea; runtime seed SHA256 fd3936a99e3629eda04843c671e3776e23e7ef82b08dc698c0a05326e489d9e7. The seed is a separate replay input identity. Native objects, preparation results, seed, bundle and all thirteen call events agree; only the Windows thread ID is excluded from fault comparison. The preceding untraced v4/v5 pair also repeats. All source image mappings remain NX, and all eight supplied module hashes are unchanged.

The independently verified image digest is taken before native runtime-word initialization: e3f0a400f1acd5302ca34718f8d8664654b5af685f9e31c889ca0694964a1901. There are nineteen logical private regions and 29 unresolved slots. Local evidence is local/runtime/canary-evidence-v1/checked.json; reports/native-runtime-word-evidence.json includes validated recorder/source ZIP identities. Current compiler v12-memory-sources, semantics v35-divide, active object manifest native-memory-manifest-v1 and registry-v7-memory-repeat are unchanged.

Continue with the mutex attribute/service interface and native stateful implementation, then investigate the next actual stop. Complete module initialization order, FP/TLS setup, callbacks, guest exceptions/nonlocal control and production helper cost remain open. P1 Hunter's Dream route, profiler overhead/CPU/GPU/queue costs and the intermittent opening audio mutex crash remain separate baseline work. No user action is needed.
