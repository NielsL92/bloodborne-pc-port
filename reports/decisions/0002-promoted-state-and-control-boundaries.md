# Decision 0002: promoted State and explicit control boundaries

2026-09-05. Supersedes the stop-expansion decision in 0001 for P3 analysis only.

Continue with Remill/LLVM. State promotion on the existing full-state contracts demonstrates mitigation of repeated register/flag/PC stores; representative longer kernels are below the approximate 2x warning threshold. Preserve the one-element warning and all previous failed results.

The service callback/nonlocal protocol, thread-local State/TLS, serialized locked operation, MFENCE publication fixture, and real five-way jump table now have bounded demonstrations with original code pages non-executable. Proceed to a provenance-aware P3 control-flow/dependency database.

Do not interpret this as authorization to execute unrecovered game targets through original bytes, or as a complete runtime. Unknown targets fail with diagnostics. Memory lowering and promotion retain their explicit contracts; recoverable asynchronous faults, MMIO, arbitrary guest mapping, and complete service coverage must be resolved before supported native gameplay.

The static-reassembly comparison remains separate and is not the endpoint. The shadPS4 file-sharing correction is a measured baseline/runtime bug fix, not native recompilation.

Service-site continuation (2026-09-06 17:41 UTC): three exact module/entry/call-site contracts now remove ordinary fallthrough conditionally while adding unresolved native service-control obligations. The abort rule additionally requires its independently checked constant arguments and entry path. Generic debug-service calls retain their return path. Five exception/nonlocal quarantines and all unknown callback/indirect targets remain. These annotations and 21,173 compiled entries are static evidence; the native runtime and P3 gate remain open. See reports/startup-services.md.

Lua callback continuation (2026-09-06 18:34 UTC): independently checked source/layout and constant-store evidence adds two conditional panic-field target dependencies. The supplied Lua state has a compatible inspected LP64 prefix plus custom extensions; a default Windows public Lua layout cannot replace it. The unknown call, state/global alias, callback mutation and nonlocal destinations remain explicit. No public interpreter or game CPU execution was introduced. See reports/lua-panic.md.

Conditional ending continuation (2026-09-06 18:55 UTC): a separate, independently checked ordinary-call model covers three exact unknown CALL sites and proves conditional paths through three helpers. It is applied only at five disputed ending sites and adds five unresolved native continuation records. Generic unknown-call rejection remains strict. All 21,181 current entries compile, but native callbacks/nonlocal transfer, full linkage and exit handling remain open; P3 has not passed. See reports/conditional-control.md.

Comparison continuation (2026-09-06 20:00 UTC): actual LLVM error-path inspection exposed incorrect guest comparison fault/state behavior. The authored COMI/UCOMI replacement and fourteen complete object replacements pass repeated independent checks. Explicit SIMD fault hooks replace 49 legacy error sites; this is not native exception delivery or permission to unwind across nounwind call contracts. P3 remains open. See reports/comi-semantics.md.

Division continuation (2026-09-06 20:26 UTC): 327,680 independent authored cases validate unsigned-magnitude DIV/IDIV and 218,424 precise faults. Twenty selector names replace legacy fault-PC and signed-overflow behavior. Thirteen exact object replacements remove all generic error references from the active set. Source/exit intent, nounwind-compatible native handling and complete linkage remain open; P3 has not passed. See reports/divide-semantics.md.

Call-return continuation (2026-09-06 20:58 UTC): ordinary callee State PC is now checked before restoring the continuation. Exact nonreturn assertions retain source/provenance and fail explicitly on normal return, even if the destination is compiled. Authored tests repeat; production handling and migration of the complete game object set remain open. The pinned preexisting dynamic guard only covered asynchronous hypercalls. See reports/call-return.md.

Source-exit continuation (2026-09-06 21:35 UTC): all startup objects now carry exact nonreturn assertions and sourced transfer/fault interfaces. Per-invocation source slots survive joins/nested calls correctly in authored tests. Native dispatch requires actual/requested equality, a decoded source/target pair and validated binding, including five tail-only imports absent from COFF names. Saved bitcode is pre-clang-O2; structural reachability is not guest execution. Complete native handling/linkage remains open. See reports/source-exits.md.

Runtime boundary continuation (2026-09-06 22:13 UTC): registered private NX RAM and static native dispatch pass repeated authored checks, including negative child-process exits. Runtime faults terminate without unwinding across nounwind calls. This does not implement guest exception/nonlocal recovery or real services; FP profile selection and complete linkage remain open. Source/request authorization is enforced separately from target availability. P3 remains open. See reports/runtime-boundaries.md.

FP runtime continuation (2026-09-06 22:23 UTC): native numeric linkage and explicit FP profile/segment contracts pass repeated authored AOT checks. No target profile is inferred. Missing profile/metadata and architectural faults stop through noexcept diagnostics; guest exception delivery remains open. P3 is not yet passed. See reports/runtime-fp.md.

P3 gate continuation (2026-09-06 22:37 UTC): the complete recovered startup set links with explicit native exit handling and reproduces byte-for-byte. The plan compilation/explicit-handling gate passes; proceed to P4 private NX loading and native startup. Unknown targets, fourteen uncertain x87 selector sites, conditional export bindings, unimplemented services and guest exception/nonlocal recovery remain explicit. No game function was executed to establish this gate. See reports/native-link.md.

Native function identity continuation (2026-09-06 23:02 UTC): external function pointer relocations use one canonical logical identifier per exact NID/library/module/version. Native slot 9 is reserved and never mapped as original executable bytes. Existing PLT gateways remain source-specific aliases; real service calls stop until implemented. P4 remains open. See reports/native-services.md.

Memory source continuation (2026-09-06 23:26 UTC): an authored guarded load after NOP exposed stale trace-entry PC diagnostics. Optional compiler bridges now carry explicit State and invocation-local instruction PC into memory/FP helpers; terminating faults record that PC and successful scopes preserve State. Guards prevent unresolved-slot reads and writes before copying bytes. Whole game object migration and revalidation remain required before private native loading. P4 stays open. See reports/runtime-guards.md.

Whole memory-source migration (2026-09-06 23:42 UTC): every active startup object now uses the validated native State/source bridges. Exact repeated builds, independent LLVM argument checks and complete native linkage pass; existing control and recovery assumptions are unchanged. Object growth is recorded and helper runtime cost remains unmeasured. P4 private loading/startup remains open; no game-derived CPU function executed. See reports/native-memory-sources.md.

Private loader continuation (2026-09-06 23:55 UTC): native private NX allocations and resolved relocation application reproduce the independent unguarded image digest. Unresolved slots are inaccessible through registered helpers. This validates loading data, not startup ABI, initialization order, target FP/TLS state or game execution. P4 stays open. See reports/native-load.md.

First native entry (2026-09-07 00:15 UTC): supplied main/libc entry code executes as AOT under an explicit bounded private context and reaches the independently predicted unresolved canary-slot stop. Source bytes remain NX. The trace proves neither full startup ABI/initialization order nor boot; all unresolved guards stay active and P4 remains open. See reports/native-startup.md.

Native constructor entry (2026-09-07 01:03 UTC): an independently checked native runtime word resolves eight exact data imports. Repeated AOT startup completes both atexit registrations and enters constructor ordinal 0 before an explicit mutex-attribute service stop. Other data/TLS guards and all discovery/control uncertainty remain. P4 is open; this is not boot. See reports/native-runtime-word.md.

Native mutex attributes (2026-09-07 01:16 UTC): nine exact import identities bind three stateful native services under independently checked caller interfaces. Repeated AOT startup advances to explicit mutex creation. Opaque attribute identifiers do not claim a guest-readable private object layout; unrelated services and data/TLS remain unresolved. P4 is open. See reports/native-mutex-attributes.md.

Native mutex ownership (2026-09-07 01:30 UTC): stateful creation/lock/unlock/destruction and native-thread exclusion pass repeated authored AOT checks. Supplied startup completes constructor 0 and enters constructor 1 before a direct-memory size service stop. Private mutex layouts and other unsupported interfaces remain explicit; P4 is open. See reports/native-mutex.md.
