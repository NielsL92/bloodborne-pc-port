# Native startup shutdown checkpoint

2026-09-07 03:48 UTC. Paused at the user's request for PC shutdown. The latest verified native build completes constructors 0–1080 and enters ordinal 1081, out of 18,444 ordered initial constructors. It stops at unknown target 0x1020b7660. This constructor count is not a percentage of port completion. P4 remains open; no native title, graphics submission, game boot or playable port exists.

The 24-instruction body at 0x10207ce10 passes exact repeated CFG recovery, independent Ghidra decoding, native compilation and startup. It retains one unresolved indirect call and one unresolved tail jump; no control annotation or source-transfer pair was added. The compiler checks two ordinary call returns and sixteen sourced memory operations. Three actual trace spans show the indirect call to 0x10207ee90 returning to 0x10207ce3a, native mutex lock/unlock returning zero, and the tail target 0x10207f070 returning to the wrapper's original caller 0x1020b7458 with the correct logical stack. The mutable table and other possible targets remain unresolved. This advances startup through 700 constructors.

The following 256-entry accessor cohort passes Ghidra, two identical compiled builds, 786,432 authored AOT cases and 512 negative checks per run, followed by identical native startup runs. Of this cohort, 36 entries return in the trace and 220 remain unobserved. Conditional entry flags are preserved separately from later observations. The remaining requested target 0x1020b7660 is outside the strict accessor census, so the second selection correctly fails and stops the bounded workflow. Its failed record and the last successful continuation are preserved; it was not forced into a leaf model.

Current local/runtime/startup-v61-cohort-chain-v1-1-repeat matches v60 exactly: 61,981,184 bytes, SHA256 35ae968d1a05f67b8eae42472d8d6e8b745b0d87980c677e097d91089cd3140d. Twenty-two supplements give 408 game objects / 21,847 compiled roots. The trace contains 4,971 diagnostic events. The final dispatch has RSP 0x700000fff88 and 146,063 completed registered memory operations. Fault source zero does not identify a caller instruction. Original bytes remain NX, with thirty data/TLS guards and no original-byte CPU execution or fallback.

Use tools/extend_native_startup.py from_run 20260907-p4-cohort-chain-v1-1-startup-repeat to preserve all service, TLS, seed and supplement arguments. The completed workflow prefix is recorded in local/runtime/cohort-chain-v1/advances.json and continuation.json; its summary.json is absent because the subsequent selection failed. Do not treat the parent workflow failure as failure of the already checked v60/v61 startup pair.

The final in-flight recovery finished before shutdown: local/cfg/native-frontier-20b7660-v1 contains entries 0x1020b7660 and 0x1020bcdd0, 302 instructions, no decode issues and no pending direct dependency queue. Its delta database SHA256 is e9a9ed9ae05341f211e9354c978f789c8e025fe9f618836eaea9171af3a36141. It retains seven unknown indirect calls, 21 callback argument candidates, three imported contracts, three nonreturn annotations and two unknown restored contexts. It is pending an independent repeat, Ghidra comparison and compilation. The inherited summary.json phrase about P3 is the recovery engine's generic delta status; current phase state is here and in STATUS.md, not a retroactive revocation of the earlier narrow P3 compilation gate.

Next session: verify Git and current files, then run recover_native_frontier again into a fresh directory; run ghidra_byte_windows for the new windows; use compile_native_frontier twice and supplemental_native_manifest only after all checks pass. Investigate any independent disagreement or emitted exit with concrete evidence. Add the validated supplement by extending the v61 recorder into new startup directories (v62/v63 are available). Continue through the plan's gates. Do not assume a small-leaf cohort or a successful object is execution coverage.

Evidence is local/runtime/cohort-chain-partial-evidence-v1/checked.json, local/runtime/indirect-wrapper-evidence-v1/checked.json and local/runtime/indirect-wrapper-spans-v1/checked.json, with source archives and hashes in their run records. Every existing run and original package/game view is preserved. Compiler v12-memory-sources / semantics v35-divide, registry-v7-memory-repeat, loader-plan-v9-runtime-word, rwlock-contract-v1, main-tls-contract-v1 and the replay seed remain current.

Progress is early native runtime integration: input/tool setup, compiler feasibility and the recovered startup compilation/explicit-exit gate are established, and native startup now executes real supplied code. Most product work remains: broader service/control/TLS coverage, native graphics, playable opening, audio/input/save integration, performance comparisons, full-game coverage and packaging. The separate shadPS4 baseline reaches the clinic and supports movement/quit/reload; its Hunter's Dream route, profiler overhead/CPU/GPU/queue measurements and intermittent opening audio crash remain open.

Requested planning estimate, low confidence: roughly 2–6 months of sustained engineering to a first playable native slice, and 6–18+ months to a dependable full-game port, assuming no fundamental compiler/runtime/rendering blocker. These are provisional planning ranges, not measured delivery dates; re-estimate after a native graphics submission and a first gameplay scene. The constructor count cannot support a reliable overall completion percentage.
