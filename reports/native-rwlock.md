# Native reader/writer locks and the first TLS dependency

2026-09-07 02:15 UTC. Native reader/writer-lock initialization succeeds inside constructor ordinal 89. Startup then reaches an eight-byte FS:[0] read at 0x10207bf94. The explicit probe context still has FS base zero, so registered memory stops at address zero. This is the first reached native thread-local-storage dependency. Constructors 0–88 remain completed; P4 is open and there is no native boot or playable port.

Native reader/writer locks maintain a writer thread identity, reader counts per thread, total readers and blocked-reader/writer counts under a critical section. Condition-variable waits recheck ownership after waking. Readers share access; a writer excludes readers and other writers. Recursive readers can reenter while writers wait. New readers defer to waiting writers. This is an explicit native scheduling policy, not a measurement of console scheduling or priorities. The independent [FreeBSD implementation](https://raw.githubusercontent.com/freebsd/freebsd-src/releng/9.0/lib/libthr/thread/thr_rwlock.c) supports the recursive-read and blocking model; the [Open Group interface](https://pubs.opengroup.org/onlinepubs/7908799/xsh/pthread_rwlock_rdlock.html) permits multiple read acquisitions and discusses writer preference. Neither is a complete PS4 ABI declaration.

Opaque native identifiers occupy [0x73000000000,0x73100000000), with a 65,536-live-object policy. Initializing an eight-byte handle creates real native state. Supported initialization requires default attributes and a null name. Named, static, attribute, timed and guest-readable private-layout interfaces remain explicit stops. Invalid handles, incompatible recursive acquisition, wrong-owner unlock and busy destruction return documented native SCE diagnostics. Guest thread creation, cancellation and priority scheduling remain unimplemented.

Repeated authored AOT fixtures pass 68,630 service calls across 4,096 lifecycles, two simultaneous readers, recursive read admission, writer exclusion until the final read release, and a reader blocked until writer release. Three native workers complete exactly 1,536 protected increments. Resource exhaustion, stale handles and callee-saved/logical-return checks pass. Eight negative processes stop on missing provider, invalid/guarded pointer, unsupported attribute/name/static input, live reinitialization and opaque-layout dereference. Native objects and outcomes repeat exactly. These are authored concurrency experiments, separate from game/baseline execution.

Ghidra independently checks 164 instructions in eight bounded initialization/read/write/unlock and PLT windows. Exact bytes and Capstone lengths agree. Four SCE NIDs extend the prior contract through exact library/module/version identities; local/runtime/rwlock-contract-v1/contract.json has 41 bindings across fifteen services. Destruction and try operations are authored-tested but remain unbound pending reached caller evidence. The actual game trace observes one rwlock initialization returning zero; it does not establish game execution of read, write, unlock or destruction.

local/runtime/startup-v17-rwlock-repeat/startup.exe repeats v16-rwlock: 61,859,840 bytes, SHA256 b148b8aaca10373843bcf753fc87f931b1b3a649b7f9f14a43ef5b9d02a4ef02. Trace identity 7ddc70dcb45bd99ce7d96b14115ba77f7006e0de33ab59729aeb9c4ab22c6577. All native objects, bundle, seed and 298 diagnostic dispatch/import events repeat, excluding only Windows thread ID from the fault comparison. The FS read stops at RSP 0x700000fff00 with no active import. The last completed dispatch event reports 1,590 memory operations; this is not an exact count at the later fault, because the diagnostic stream omits statically compiled direct calls and individual memory operations.

Evidence local/runtime/rwlock-evidence-v1/checked.json verifies supplied module hashes, exact artifacts and recorded source archives. Compiler v12-memory-sources, semantics v35-divide, base registry-v7-memory-repeat and native-leaf-manifest-v1 remain current: 387 game objects / 21,283 roots. All source bytes stay NX. The 29 data/TLS guards, complete module initialization order, unknown targets, mutable tables, private layouts, FP profile, exceptions/nonlocal control and helper costs remain open. P1 route/profiling/audio work is unchanged and separate.

Next independently validate the supplied FS-relative TLS layout and offset sources, initialize private per-thread storage and the explicit State FS base, and investigate the next native stop. Do not infer the complete TCB layout from one self-pointer read or replace unresolved module references with guesses. No user action is needed.
