# Observed 64-bit accessor recovery

2026-09-07 02:35 UTC. Two more missing native targets now execute and return inside constructor ordinal 89. Target 0x102375af0 loads RAX from [RDI+0x40]; target 0x102375b00 loads RAX from [RDI+0x48]. Each supplied body consists of four MOV bytes and RET. Startup advances to the next unknown target, 0x102375b10. Constructors 0–88 remain complete. P4 is open; no native boot or playable port exists.

Each body is requested by an actual native dispatch, recovered into a fresh bounded record, independently decoded by Ghidra, and compiled through compiler v12-memory-sources / semantics v35-divide. Padding and neighboring entries are excluded. Unwind and pointer-candidate metadata are retained with their original uncertainty. Independent LLVM inspection validates sourced memory arguments and root identity, and COFF checks the single exported root and supported helper references. Two builds per body reproduce exact input, audit, bitcode, object, inspection and fixture artifacts.

The generalized scalar-load fixture supports the independently checked MOV AL,[RDI+disp8] and MOV RAX,[RDI+disp8] forms followed by ordinary RET. It checks 3,072 input cases per body, varying supplied values, initial RAX and object placement including the last mapped bytes. Results, selected flags, callee-saved registers and logical return/RSP are checked. Unmapped and unresolved loads stop at the exact source instruction. The earlier byte body also passes this generalized fixture and retains identical input/audit/bitcode/object hashes. These game-derived AOT function experiments use authored inputs; they do not establish discovery or whole-game execution coverage.

The current probe uses three explicit additive manifests: native-leaf-manifest-v1, native-qword-leaf-manifest-v1 and native-qword48-leaf-manifest-v1 under local/compiler-spike. The combined manifests contain 389 game objects and 21,285 roots. Base recovery and registry records remain preserved. Each extension contributes one independently checked entry and no imports or source/request transfer pairs. Full combined target/object lists enter the probe identity and native table validation.

local/runtime/startup-v23-qword48-leaf-repeat/startup.exe repeats v22-qword48-leaf: 61,860,864 bytes, SHA256 6634396dd8d0c2fc39781f54df6416d85ee528191185358edb64c41a3c482096. Trace identity 8d1ddecf3134af8a3c9bc0292c731f8bb8e9058960bac5a6a0c3441b23a5e9b0. All native objects, bundle, seed and 359 diagnostic dispatch/import events repeat, excluding only Windows thread ID from fault comparison. The first accessor-only checkpoint v20/v21 also repeats. Both recovered 64-bit accessors return in the actual trace. The next unknown target has RSP 0x700000ffea8 and 2,035 completed registered memory operations. The stream does not enumerate every compiled direct call or instruction.

Evidence local/runtime/native-qword-leaves-evidence-v1/checked.json and the report JSON verify artifacts and recorded sources. Main static TLS, rwlock-contract-v1 and loader-plan-v9-runtime-word remain current. All original bytes remain NX; 29 prior unresolved data/TLS guards plus the TCB envelope guard remain. Mutable tables, full initialization/FP/dynamic-TLS behavior, private layouts, unknown targets, exceptions/nonlocal control and helper costs remain open. P1 route/profiling/audio work is unchanged and separate. Next independently compile the observed constant-address accessor 0x102375b10; no user action is needed.
