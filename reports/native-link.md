# Complete native linkage and P3 gate decision

Current generation (2026-09-06 23:42 UTC): see reports/native-memory-sources.md. Active objects are native-memory-manifest-v1; current registry is registry-v7-memory-repeat, link is link-v9-memory-repeat and loader plan is loader-plan-v8-memory. Complete authored/source/LLVM/link checks pass; native game loading/execution has not occurred.


2026-09-06 22:37 UTC. **P3 startup compilation and explicit-exit-handling gate passes. P4 remains open. No game-derived CPU function has been executed, no native game boot exists, and there is no playable port.**

The plan's P3 gate requires the recovered startup/next-milestone call graph to compile with explicit handling for discovered exits while preserving unknown/unvisited coverage. The complete 386-object set now links with 21,282 compiled roots, all 18,444 initial constructors retained exactly once, 182 import gateways and all 63 support/library names resolved. The static registry carries 551 independently decoded source/request pairs. Runtime handling and diagnostic stops have authored AOT evidence; the full linked registry has host-only structural validation and negative gateway probes. This passes compilation and explicit handling, not implementation of every service, successful startup or complete static discovery.

Current executable: local/runtime/link-v5-repeat/registry.exe, 54,663,680 bytes, SHA256 307f10134ca4236b89ead17006b72b80c14af2617a3c5321a9df1ed4bae8190b. It has no game-run mode. Host validation calls no game entries and creates no guest mappings or FP profile. Native import probes create an empty sealed address-space context and call only the generated native gateway. All 115 external gateways stop with the expected stub PC, NID, library, module and compilation identity. An unknown-target probe stops. A --run-game argument is rejected. The other 67 gateways bind to a unique supplied compiled export with matching NID/library/module; their execution still depends on module loading, relocation, initialization and interposition assumptions. No service is replaced with a success placeholder.

Registry-v3-repeat matches registry-v2-ghidra for all ten generated artifacts. Link-v5-repeat matches link-v4-import-stops for eight native objects, the complete PE, linked-address table, native-import stop results and host validation output. Twenty-two artifacts match in total. The linker map accounts for every compiled root and import gateway at a distinct native executable-section address. A raw PE check rejects writable executable sections. /OPT:NOREF retains inputs and /OPT:NOICF avoids folding distinct entry addresses; reproducible linking removes timestamps. This does not establish that every machine path was exercised. All exact game objects remain the source-exit-manifest-v2-regression set; no superseded or diagnostic game objects were re-added.

The generated registry lives in local/runtime/registry-v3-repeat. Identity SHA256 e73b23f8b12cad14b6352ef944f3b190a1649de1d0ab07a231ea9564e311d7c4 covers exact object/input/audit/bitcode identities, targets, imports, source/request pairs, module identities and x87 metadata. Active manifest SHA256 remains e5c5516e4703ac055f97128799b45bed91c3b90d235a67f6446973c3a698a8a6. Native core and numeric source hashes match the authored FP runtime checkpoint. All original packages and hardlinked views are unchanged.

Fourteen of 349 x87 metadata sites have unresolved default-segment selection for R12/R13-based operands. Ghidra independently confirms all fourteen exact bounded instruction decodes and base registers. Its long-mode memory model does not resolve this legacy selector question, so these fourteen are omitted from the 335-entry runtime metadata table and stop explicitly if reached. The generator does not guess a selector. The other records contain 42 DS, 62 SS and 231 register-only forms. No PS4 FP profile is selected. Public documentation/source searches did not provide an independently sufficient target-profile answer; local Ghidra and Remill source checks are recorded as model evidence, not console proof.

Other remaining unknowns are unchanged: 9,044 indirect calls, 430 indirect jumps, 143 callback arguments, five conditional continuations and three service-control obligations. Conditional callback/object/RTTI/Lua bindings, mutable tables and unvisited targets remain visible in recovery v31-conditional-repeat. Runtime process stops are compatible with nounwind but do not implement guest exception/nonlocal recovery. These are explicit P4 obligations, not erased by linking or gate passage.

Preserved failures: native-link-v1 failed at the compiler driver's /MAP response-file handling; v2-direct successfully linked and validated the host registry, then the map checker failed because LLD does not emit the MSVC-style function marker. V3-map fixed the parser and checked native section membership. The final v4/v5 runs additionally probe every external gateway and reproduce exactly. No linker errors were hidden or disabled.

Reproduce with tools/run_record.py and fresh output directories:

- tools.native_registry local/compiler-spike/source-exit-manifest-v2-regression/active-objects.json local/compiler-spike/source-exit-dispatch-v2-tail-imports local/cfg/startup-recovery-v31-conditional-repeat OUT --ghidra-segments local/cfg/ghidra-extended-segments-v1
- tools.native_link REGISTRY build/softfloat-v2-repeat/softfloat.lib OUT
- tools.summarize_native_link OUT (checks the pinned checkpoint runs)

Next implement P4 module loading as private NX data, exact relocation/BSS and initialization order, logical stack/TLS setup, then start from the actual entry with explicit diagnostics. Implement native services in observed dependency order and validate callbacks/nonlocal transfer separately. P4 passes only at the plan's actual startup/graphics milestone. The P1 Hunter's Dream route, profiling and intermittent opening audio mutex investigation remain separate and open.
