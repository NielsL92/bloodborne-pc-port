# Canonical native function identities

2026-09-06 23:02 UTC. P4 is open. P3 compilation/explicit handling passed. No game-derived CPU function has executed and no native boot/playable port exists.

The new manifest assigns 593 canonical native function addresses to exact NID/library/module/version identities. It covers 796 import-symbol uses and 831 relocation uses, with 143 identities shared across modules. Every corresponding relocation now receives the same logical function identity (plus an R_X86_64_64 addend where applicable). The generator rechecks original module hashes, actual symbol fields, versions and each relocation use. The same external function no longer acquires a different function-pointer value from each importing module's PLT address. Real service implementations remain explicit stops.

Logical address range [0x900000000,0xa00000000) is reserved for these native service identifiers. Addresses are deterministic, sixteen bytes apart, and overlap none of the eight supplied module regions. They have no original instruction bytes or executable data mapping. Future dynamic module allocation must skip this reserved slot. Imported function-pointer equality follows exact versioned identity; actual game use remains untested.

Current registry: local/runtime/registry-v5-service-identities, identity c6682220b81ce1f78abce9da0daaaee45efaa954027d8fab7e8c1264301efa3b. It retains all 21,282 game roots and adds 593 canonical gateways to the previous 182, giving 775 import entries. The 67 supplied compiled-export bindings remain conditional. All 708 unimplemented import entries stop with matching identity in linked child-process probes. A separate unknown-target probe stops. These probes execute native gateway/runtime code only.

Current executable: local/runtime/link-v7-repeat/registry.exe, 54,722,048 bytes, SHA256 98bc1df33f8b15d5b42d8807459897f2686bd4b6829cf0d130914a9733f19b99. It matches link-v6-services byte-for-byte, including all runtime objects and stable linked-address/probe results. Service manifests, generated registries and loader plans also repeat exactly; the combined checker verifies 50 artifacts and all 831 relocation identities. No game entry or guest mapping is created by this validator.

Loader-plan-v7-repeat now has 37 unresolved relocation references: twenty strong data imports and seventeen TLS module references. All 238,609 destinations and 18,444 constructor pointers remain checked; zero relocations write into code. The initial module set, compiled objects, compiler, semantic module, mutable-table assumptions, unknown targets and fourteen unresolved x87 selector sites remain unchanged. Service metadata includes explicit library/module versions; version-specific implementation remains future runtime work.

Next add runtime guards for unresolved relocation slots before private module loading. Reads or writes overlapping such slots must stop with precise provenance before an unresolved value can influence execution. Do not replace missing strong-data/TLS values with zero or guessed addresses. Then validate private loading, strong-data/TLS contracts and actual entry setup. Guest callbacks, exception/nonlocal behavior and real native services remain P4 work; P1 baseline tasks remain separate.

Reproduce through tools/run_record.py in fresh output directories: tools.native_services REGISTRY OUT; tools.native_registry with --native-services local/runtime/services-v2-repeat and the existing Ghidra evidence; tools.native_loader_plan with the expanded registry and checked weak bindings; tools.native_link; and tools.summarize_native_services OUT for this checkpoint's pinned comparisons.
