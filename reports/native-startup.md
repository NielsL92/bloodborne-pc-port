# First bounded native AOT entry trace

Continuation 2026-09-07 01:03 UTC: the runtime-word binding now advances through both atexit registrations into the first constructor, stopping at mutex-attribute initialization. See [native-runtime-word.md](<E:/bloodborne PC port/reports/native-runtime-word.md>). The original first-stop evidence below remains preserved.

2026-09-07 00:15 UTC. **The supplied main entry and its first libc calls now execute as native Remill/LLVM AOT code.** The repeat reaches the independently predicted unresolved canary-slot stop. P4 remains open; there is no native game boot, graphics submission or playable port. No original game bytes execute.

The first native entry run was 20260907-p4-native-startup-v1 (00:07:50–00:08:11 UTC). Main entry 0x1000000a0 executes its prologue and argument-prefix loads, calls supplied libc _init_env at 0x80005fef0 (compiled RET), returns through the checked continuation, and calls compiled atexit at 0x80002f110. The latter stops on its first canary-pointer load, instruction 0x80002f119, reading unresolved relocation slot 0x8000b84e0. The slot is libkernel object NID f7uOxY9mM1U, named __stack_chk_guard in the independent baseline symbol catalog. Its semantic value and native storage remain unresolved.

Six bounded Ghidra windows independently check the main initializer/entry prefixes, two PLT jumps and both initial libc bodies. All 82 decoded instructions match supplied bytes and Capstone lengths; recorded recovery instructions in those windows agree. These windows do not prove complete function extents or startup order. The atexit RIP-relative slot is independently decoded by Ghidra and Capstone. Contract evidence is local/runtime/startup-entry-contract-v1/contract.json, SHA256 52e30b7de4e1eacbae25436a550a1c32d78745a382b306858c21939f846c348b.

The probe uses an explicit private process context: a 1 MiB logical stack at 0x70000000000 with entry RSP 0x700000fffe8 (alignment 8 modulo 16), argc=1 and inline argv pointers at 0x70000200000, argv[0]=/app0/eboot.bin, and a registered native diagnostic exit callback at 0x9fffffff0. The supplied entry directly establishes the argc offset/width and argv offset. The full parameter allocation and initial stack packing use the separately identified baseline reference; they are not a proven complete console startup ABI. Other State fields are zeroed. No target FP profile or per-thread TLS is selected; the checked prefix uses neither. The exit callback stops explicitly if called and does not implement process teardown.

Before entry, the loader validates eighteen private NX regions totaling 98,574,336 bytes, including the authored stack/argument data. All 98,574,040 unguarded bytes match the independent image digest 2c8e2b58fe050a26ab16ee81fdae8a8741f62ce332f83b36a3b4e31eb0a1b501. The 37 unresolved slots stay guarded. The complete PE contains all 21,282 current game roots and 775 import gateways at distinct native executable-section addresses. No writable executable PE section or executable source-byte mapping is used.

The actual stop reports source/PC 0x80002f119, address 0x8000b84e0, width eight, RSP 0x700000fff80 and active import atexit (NID 8G2LB+A3rzg, gateway 0x102bbe4c8). Eleven registered memory operations complete before the guarded read; the read itself does not complete. The 104-byte RSP decrement and every logical fault field match the independently predicted prefix. Callback registration, constructor calls and main have not been reached. No service success placeholder advances the trace.

Current portable probe: local/runtime/startup-v3-repeat/startup.exe, 61,802,496 bytes, SHA256 001058c6fb85e8f65ac3524d19cf7c45396e86702aac71658d9daaf679e4b5f6. It matches startup-v2-portable exactly, including native objects, bundle, preparation results and logical fault fields. Only the Windows thread ID is excluded from trace equality. startup.bin has SHA256 50c451119975da769a92b5c9bf97f294e60dd2c001ed47d9424f35e16e1df05c; trace identity 5b4745457db251e65336dd81f426289c8d54f67a3cf9ffccada1fb14d75129ba. The original v1 run is preserved; v2 locates its bundle beside the executable and adds raw PE/map checks. Ordinary --run-game remains rejected; this binary exposes only preparation and the bounded probe.

All compiler/registry/recovery identities remain as recorded in reports/native-memory-sources.md. Evidence: reports/native-startup-evidence.json and local/runtime/startup-evidence-v1/checked.json, with verified run/source snapshots. The bundle-format helper extraction reproduces the prior loader bundle and digest exactly.

Next investigate a native __stack_chk_guard data-object contract, its exact namespace/version, pointer width, initialization and mutability; independently validate the binding before removing those specific guards. Preserve all other strong-data/TLS unknowns. Continue service/startup work from the resulting concrete stop. Complete module initialization order, FP/TLS profile, callbacks, guest exceptions/nonlocal transfers and helper runtime cost remain open. P1 baseline route/profiling/audio work is unchanged and distinct from this native trace.
