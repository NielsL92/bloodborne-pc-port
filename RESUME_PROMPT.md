Continue E:/bloodborne PC port/PLAN.md within its approved scope. This is native port development. All no-cost work and read actions are approved; no PS4 access is available. Verify the current Git workspace before editing. The native execution checkpoint is f4ad5e5; a later documentation-only commit adds development-acceleration advice without changing the build.

Read the current top sections of HANDOFF.md and reports/STATUS.md, then reports/native-startup-checkpoint.md, reports/native-startup-checkpoint-evidence.json, reports/development-acceleration.md, reports/native-leaf-cohorts.md, reports/native-cfg-frontier.md and reports/decisions/0002-promoted-state-and-control-boundaries.md. Older checkpoint sections are history, not competing continuation instructions. Read large evidence files selectively using their referenced keys and records.

The project was paused for shutdown. Latest repeated native checkpoint: local/runtime/startup-v61-cohort-chain-v1-1-repeat. It completes 1,081 constructors and stops at 0x1020b7660. P4 is open; there is no native game boot or playable port.

First finish the pending recovery local/cfg/native-frontier-20b7660-v1 (two entries / 302 instructions): repeat in a fresh directory, cross-check with Ghidra, compile and repeat with tools/compile_native_frontier.py, then publish an additive manifest only after its checks pass. Use tools/extend_native_startup.py from_run 20260907-p4-cohort-chain-v1-1-startup-repeat to retain every supplement/service/TLS argument. Native startup v62/v63 are available output names, subject to workspace verification.

The user asked to reduce development time. Apply the following priorities within the existing gates, measuring benefit rather than building a new framework:

1. Connect the existing recovery paths into one resumable, bounded driver: classify the actual stop, route supported leaves to cohort recovery and other recoverable bodies to the existing CFG pipeline, perform independent checks, publish the supplement, repeat native startup, and save continuation after each accepted step. An out-of-census target is a routing decision; real decode/compiler/contract disagreements remain failed gates to investigate. Preserve prior failed records. Respect the current single-module / 64-entry CFG compiler limit and current restrictions on new imports and source pairs; do not raise limits blindly. Validate the driver against saved leaf, CFG, and failure cases before unattended use.

2. Rank missing dependencies by actual native observations, supplied data references, and targeted baseline observations. Use bounded batches of supported candidates and record conditional versus observed entries separately. The initial leaf census contains 9,760 candidates, but completing that census is not the next milestone. Work toward native startup, real graphics submission, and the opening route.

3. Use the separate, already authorized instrumented shadPS4 baseline as a discovery aid where it saves repeated native stops. Capture bounded site/target, callback, service, module-load, and relevant table-write observations around startup or the clinic. Deduplicate and identify them by module hash and RVA. Its previous 63.9-million-zone trace shows why whole-program tracing is a poor default. Baseline observations are hypotheses to recover and check, never native execution evidence or proof of complete/mutation-free tables. Keep P1 route/profiling/audio results distinct.

4. Measure before optimizing the build workflow. The last successful 256-entry cohort plus the next rejected selection took 74.18 seconds of recorded command time; Ghidra 12.29 s, compilation pair 26.07 s, startup pair 31.69 s. Startup rebuilds sixteen native translation units. Consider caching unchanged runtime objects with complete content/toolchain/header/flag keys and bounded parallel independent jobs, only if measured worthwhile. Keep fresh run directories and explicit artifact provenance. Do not present cache reuse as an independent rebuild or remove required cold repeats. Profile database setup/hashing before redesigning storage.

5. Reuse selected pinned shadPS4 service and GPU components behind explicit native adapters, as PLAN.md already specifies. Prepare coherent service families and dependency inventories rather than one guessed stub at a time. Verify ABI, State, memory, TLS, callbacks, ownership, errors and threading. The linked native product must retain no path to original-game CPU execution. Defer renderer rewrites, speculative CPU optimization and 60-fps work until their planned milestones.

6. Make repeated failures easier to diagnose: machine-readable fault classification, exact continuation commands, compact evidence summaries and dependency lists. Source zero is currently unknown, not an inferred caller. Consider sourced indirect-call diagnostics only if repeated ambiguity justifies the compiler migration. Before automating exception-bearing cases, audit tools/ghidra_byte_windows.py: it currently drops additional_roots although the Java checker supports them. Preserve Ghidra independence and validate any correction on a relevant fixture.

Finish the pending native step first, then time-box the smallest useful driver improvement and demonstrate progress through both supported recovery paths. Prioritize engineering time saved and milestone progress; constructor counts alone cannot support a completion estimate. Overlap independent build/research work only when resources and active instructions permit. Do not start multiple writers against a shared analysis database or use parallel runs as performance benchmarks.

Preserve uncertainty and every existing run. Use fresh directories and tools/run_record.py. Never modify original packages or hardlinked game views. Native AOT remains the deliverable: no emulator/reassembly/interpreter/JIT fallback or original-game CPU execution in the native product. Keep baseline observations separate. Existing tools work; approved require_escalated shell calls work around the broken normal sandbox helper, so do not request another restart or repeated read confirmations.

Maintain HANDOFF.md, STATUS.md, evidence and source commits. Continue through the plan's gates until I ask you to stop or an actual user action is necessary. Independent P1 route/profiling/audio work remains open.
