Continue E:/bloodborne PC port/PLAN.md within its approved scope. This is native port development. All no-cost work and read actions are approved; no PS4 access is available. Verify the current Git workspace before editing.

Read HANDOFF.md, reports/STATUS.md, reports/native-startup-checkpoint.md, reports/native-startup-checkpoint-evidence.json, reports/native-leaf-cohorts.md, reports/native-cfg-frontier.md and reports/decisions/0002-promoted-state-and-control-boundaries.md first. The current checkpoint supersedes earlier startup counts.

The project was paused for shutdown. Latest repeated native checkpoint: local/runtime/startup-v61-cohort-chain-v1-1-repeat. It completes 1,081 constructors and stops at 0x1020b7660. P4 is open; there is no native game boot or playable port.

Pending recovery local/cfg/native-frontier-20b7660-v1 has two entries / 302 instructions. Next repeat that recovery in a fresh directory, cross-check with Ghidra, compile and repeat with tools/compile_native_frontier.py, then publish an additive manifest only after its checks pass. Use tools/extend_native_startup.py from_run 20260907-p4-cohort-chain-v1-1-startup-repeat to retain every supplement/service/TLS argument. Native startup v62/v63 are available output names.

Preserve uncertainty and every existing run. Use fresh directories and tools/run_record.py. Never modify original packages or hardlinked game views. Native AOT remains the deliverable: no emulator/reassembly/interpreter/JIT fallback or original-game CPU execution. Keep baseline observations separate. Current tools work; approved require_escalated shell calls work around the broken normal sandbox helper, so do not request another restart or repeated read confirmations.

Maintain HANDOFF.md, STATUS.md, evidence and source commits. Continue through the plan's gates until I ask you to stop or an actual user action is necessary. Independent P1 route/profiling/audio work remains open.
